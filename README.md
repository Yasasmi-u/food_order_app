# 🍽️ foodie — Premium Online Food Ordering System

A high-end, full-stack Online Food Ordering System built with **Spring Boot** and **React TypeScript**. This project features a state-of-the-art **Glassmorphism UI**, dynamic animations, and a secure JWT-based authentication system.

---

##  Design Highlights
- **Premium Aesthetics**: A sleek "Light Background / Glass Card" system with vibrant amber accents.
- **Typography**: Powered by the professional **Plus Jakarta Sans** typeface.
- **Glassmorphism**: Translucent panels with backdrop-blur effects and soft shadows.
- **Dynamic Interactions**: Micro-animations and hover effects for a premium "app-like" experience.

---

## Tech Stack

### Backend (Spring Boot)
- **Java 17** & **Spring Boot 3.2.5**
- **Spring Security** with **JWT Authentication**
- **Spring Data JPA** (MySQL 8)
- **Lombok** for clean code
- **Gradle** for dependency management

### Backend — Bonus (MERN Stack)
- **Node.js** & **Express.js**
- **MongoDB** with **Mongoose**
- **JWT Authentication** (jsonwebtoken)
- **bcryptjs** for password hashing

### Frontend
- React 18 (TypeScript)
- React Router DOM
- Axios
- Tailwind CSS
- Vite

---

## Project Structure

```
food_order_app/
├── backend/          ← Spring Boot API (MySQL)
│   └── backend/
│       └── src/main/java/com/foodapp/backend/
│           ├── config/        ← Security config
│           ├── controller/    ← REST controllers
│           ├── dto/           ← Request/Response DTOs
│           ├── entity/        ← JPA entities
│           ├── exception/     ← Global exception handler
│           ├── repository/    ← JPA repositories
│           ├── security/      ← JWT filter & util
│           └── service/       ← Business logic
├── mern-backend/     ← Node.js + Express + MongoDB (Bonus)
│   ├── models/        ← Mongoose models
│   ├── routes/        ← Express routes
│   ├── middleware/    ← JWT middleware
│   ├── seed.js        ← MongoDB seed script
│   └── server.js      ← Entry point
└── frontend/         ← React App (works with both backends!)
    └── frontend/
        └── src/
            ├── api/           ← Axios API calls
            ├── components/    ← Sidebar, Layout
            ├── context/       ← Auth context
            ├── pages/         ← Login, Home, Menu, Cart, Orders, Products
            └── App.tsx        ← Routes
```

---

## Database Setup

### MySQL (Spring Boot)
1. Create a database named `food_app` in your MySQL environment:
   ```sql
   CREATE DATABASE food_app;
   ```
2. The tables are automatically generated by Hibernate on first run (`ddl-auto: update`).
3. Run the provided `db_seed.sql` to populate initial categories and food items.

### MongoDB (MERN — Bonus)
1. Make sure MongoDB is running:
   ```bash
   net start MongoDB
   ```
2. Seed the database:
   ```bash
   cd mern-backend
   node seed.js
   ```

### Default Users
| Role | Username | Password |
| :--- | :--- | :--- |
| **ADMIN** | `admin` | `1234` |
| **CUSTOMER** | `Sandun` | `1234` |
| **CUSTOMER** | `sachini` | `1234` |

---

## Backend Setup — Spring Boot (`/backend/backend`)

1. Open in IntelliJ IDEA or your preferred Java IDE.
2. Ensure your `application.properties` matches your local MySQL settings:
   ```properties
   spring.datasource.url=jdbc:mysql://localhost:3306/food_app
   spring.datasource.username=root
   spring.datasource.password=
   ```
3. Run the application via `BackendApplication.java` or use:
   ```bash
   ./gradlew bootRun
   ```
   *Server starts on `http://localhost:8080`*

---

## Backend Setup — MERN Bonus (`/mern-backend`)

1. Install dependencies:
   ```bash
   cd mern-backend
   npm install
   ```
2. Create a `.env` file:
   ```env
   PORT=3000
   MONGO_URI=mongodb://localhost:27017/food_app_mern
   JWT_SECRET=my-very-strong-secret-key-for-jwt-256-bit
   JWT_EXPIRATION=3600000
   ```
3. Seed the database:
   ```bash
   node seed.js
   ```
4. Start the server:
   ```bash
   npm run dev
   ```
   *Server starts on `http://localhost:3000`*

---

## Frontend Setup (`/frontend/frontend`)

1. Install dependencies:
   ```bash
   cd frontend/frontend
   npm install
   ```

2. Choose which backend to connect to:

   **For Spring Boot (port 8080):**
   ```bash
   copy .env.springboot .env
   ```

   **For MERN (port 3000):**
   ```bash
   copy .env.mern .env
   ```

3. Start the development server:
   ```bash
   npm run dev
   ```

4. Access the app at **`http://localhost:5173`**

> The same React frontend works with **both** backends — just switch the `.env` file!

---

## API Endpoints

### Auth
| Method | Endpoint | Description | Auth Required |
| :--- | :--- | :--- | :--- |
| POST | `/auth/signup` | Register user | **None** (Public) |
| POST | `/auth/login` | Login & get token | **None** (Public) |

### Food Items
| Method | Endpoint | Description | Auth Required |
| :--- | :--- | :--- | :--- |
| GET | `/foods` | Get all food items | **Yes** (JWT) |
| POST | `/foods` | Add food item | **Yes** (Admin) |
| GET | `/foods/category/{id}` | Get by category | **Yes** (JWT) |
| DELETE | `/foods/{id}` | Delete food item | **Yes** (Admin) |

### Categories
| Method | Endpoint | Description | Auth Required |
| :--- | :--- | :--- | :--- |
| GET | `/categories` | Get all categories | **Yes** (JWT) |
| POST | `/categories` | Add category | **Yes** (Admin) |

### Cart
| Method | Endpoint | Description | Auth Required |
| :--- | :--- | :--- | :--- |
| GET | `/cart/{userId}` | View cart | **Yes** (JWT) |
| POST | `/cart/add` | Add to cart | **Yes** (JWT) |
| DELETE | `/cart/remove/{id}` | Remove item | **Yes** (JWT) |
| DELETE | `/cart/clear/{id}` | Clear cart | **Yes** (JWT) |

### Orders & Payments
| Method | Endpoint | Description | Auth Required |
| :--- | :--- | :--- | :--- |
| POST | `/orders/place/{id}` | Place order | **Yes** (JWT) |
| GET | `/orders/{id}` | Get user orders | **Yes** (JWT) |
| POST | `/payments/{id}` | Make payment | **Yes** (JWT) |

---

## Frontend Pages

| Page | Path | Description |
|---|---|---|
| Login/Sign Up | /login | Authentication page |
| Home | / | Dashboard with stats |
| Menu | /menu | Browse food with category filter |
| Cart | /cart | View cart & place order |
| Order History | /orders | Track order status |
| Products | /products | Food list (Admin can add/delete) |

---

## Features

- JWT-based authentication (Sign Up / Sign In)
- Role-based access (ADMIN / CUSTOMER)
- Browse food items with category filter and search
- Add to cart / remove from cart / clear cart
- Place orders from cart
- View order history with status tracking
- Admin can add and delete food items
- Responsive UI with Tailwind CSS
- Global exception handling
- CORS configured for frontend-backend communication
- **Bonus:** MERN stack backend (Node.js + Express + MongoDB) compatible with the same React frontend

---

## 👨‍💻 Author

**W.R.U.Yasasmie**  
CMJD Batch 112/113 — IJSE